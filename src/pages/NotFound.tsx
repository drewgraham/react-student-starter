import PageHeader from '../components/PageHeader';

const NotFound = () => (
  <>
    <PageHeader title="Page not found" />
    <p>We could not find that page. <a href="/">Return home</a>.</p>
  </>
);

export default NotFound;
